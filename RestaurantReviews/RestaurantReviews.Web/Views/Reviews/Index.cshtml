@model IEnumerable<RestaurantReviews.Data.Review>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.revscore)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Restaurant.restname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.revscore)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Restaurant.restname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.revid }) |
            @Html.ActionLink("Details", "Details", new { id=item.revid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.revid })
        </td>
    </tr>
}

</table>
